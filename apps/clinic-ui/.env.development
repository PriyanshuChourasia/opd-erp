# Development environment — Vite loads this when running `vite` or `vite dev`
# The Vite dev server proxies /api → http://localhost:4000 (see vite.config.ts)
VITE_API_BASE=/api
VITE_APP_NAME=Doctor ERP
VITE_DEV_CONTACT_EMAIL=dev@doctorerp.local
