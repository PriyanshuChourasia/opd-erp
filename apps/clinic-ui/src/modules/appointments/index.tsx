import { CalendarClock } from "lucide-react";
import { ComingSoon } from "@/layouts/coming-soon";

export const AppointmentsPage = () => (
  <ComingSoon icon={CalendarClock} title="Appointments" description="Booking, scheduling, and the doctor slot picker will live here once the appointments API is built." />
);
