import { Package } from "lucide-react";
import { ComingSoon } from "@/layouts/coming-soon";

export const DispensingPage = () => (
  <ComingSoon icon={Package} title="Dispensing" description="Pharmacy dispensing records will live here once the dispensing API is built." />
);
