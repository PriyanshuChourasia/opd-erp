// The appointments table (booking sheet, filters, pagination, and status
// actions) is shared with the /appointments dashboard route. See
// `modules/appointments/components/appointments-page.tsx` for the implementation.
export { AppointmentsPage as PosAppointmentsPage } from "@/modules/appointments/components/appointments-page";
