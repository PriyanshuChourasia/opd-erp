import { ClipboardList } from "lucide-react";
import { ComingSoon } from "@/layouts/coming-soon";

export const PrescriptionsPage = () => (
  <ComingSoon icon={ClipboardList} title="Prescriptions" description="Consultation notes, vitals, and prescriptions will live here once the prescriptions API is built." />
);
