# Task 1 — Schema & migration: MedicineGroup, Unit, and stock fields on Medicine

Date: 2026-08-29
Status: Completed

## Task Given

Decisions already locked in: backend = apps/api (NestJS/Prisma); approach = additive only — add alias, groupId, unitId, openingStock, currentStock onto the existing Medicine model, plus two new lookup models MedicineGroup and Unit, without touching any existing field.

Prompt 1 — Schema & migration
Add MedicineGroup and Unit models to apps/api/prisma/schema.prisma (uuid id, unique name, createdAt/updatedAt, createdById/updatedById audit relations, @@index, matching the existing Specialization/DiagnosisSystem convention). Add alias String?, groupId String?, unitId String?, openingStock Decimal? @db.Decimal(12,2) @default(0), currentStock Decimal? @db.Decimal(12,2) @default(0), and relations group MedicineGroup? / unitMaster Unit? to the existing Medicine model. Generate and apply a migration named add_medicine_groups_units_stock.

## Task Accomplished

- Added `MedicineGroup` model to schema.prisma: uuid id, unique name, description, isActive, createdAt/updatedAt, createdById/updatedById audit relations with User, medicines relation, @@index on createdById/updatedById.
- Added `Unit` model to schema.prisma: uuid id, unique name, symbol, description, isActive, createdAt/updatedAt, createdById/updatedById audit relations with User, medicines relation, @@index on createdById/updatedById.
- Added 5 new fields to `Medicine` model: alias String?, groupId String?, unitId String?, openingStock Decimal? @db.Decimal(12,2) @default(0), currentStock Decimal? @db.Decimal(12,2) @default(0).
- Added 2 new relations on `Medicine`: group MedicineGroup? and unitMaster Unit?.
- Added @@index on Medicine for alias, groupId, unitId.
- Added audit relation arrays on User model for MedicineGroup and Unit (Created/Updated).
- Generated migration `20260829044814_add_medicine_groups_units_stock` containing CREATE TABLE for both tables, ALTER TABLE "Medicine" for new columns, indexes, and foreign keys.
- Applied migration successfully — database schema is up to date.
- `npx prisma validate` passed. `npx prisma generate` completed successfully.

## Files Changed

- `apps/api/prisma/schema.prisma` — Added MedicineGroup model, Unit model, 5 new fields + 2 relations on Medicine, audit relations on User
- `apps/api/prisma/migrations/20260829044814_add_medicine_groups_units_stock/migration.sql` — Auto-generated migration with CREATE TABLE, ALTER TABLE, indexes, foreign keys

## Notes

None.
