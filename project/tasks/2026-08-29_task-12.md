# Task 12 — Add /queue route in clinic-ui

Date: 2026-08-29
Status: Completed

## Task Given

Create apps/clinic-ui/src/routes/_appointments/queue/index.tsx (same auth/layout pattern as _appointments/appointments/index.tsx) rendering QueuePage from @/modules/queue directly, full-page (not nested in a tab).

Follow-up verification: Navigate to /queue directly and confirm QueuePage renders standalone with the shared sidebar/header, no console errors, no leftover Appointments-tab chrome.

## Task Accomplished

- Created `apps/clinic-ui/src/routes/_appointments/queue/index.tsx` rendering QueuePage directly.
- Route uses the same `_appointments` layout (auth guard + AppointmentsLayout).
- Route tree regenerated via `@tanstack/router-cli`.
- Updated `route-map.ts` to include `/queue` route mapping.

## Files Changed

- `apps/clinic-ui/src/routes/_appointments/queue/index.tsx` — Created (queue route)
- `apps/clinic-ui/src/modules/help/data/route-map.ts` — Added /queue route mapping
- `apps/clinic-ui/src/routeTree.gen.ts` — Regenerated (auto)

## Notes

None.
