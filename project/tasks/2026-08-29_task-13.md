# Task 13 — Move switcher into header, strip in-page tabs

Date: 2026-08-29
Status: Completed

## Task Given

In appointments-layout.tsx, add a small nav switcher between the sidebar trigger and the HelpTip — two links/buttons ("Appointments", "Queue") that route to /appointments and /queue, highlighting the active one (TanStack Router Link/useMatchRoute or useLocation, not local state). In appointments-page.tsx, remove the Tabs/TabsList/TabsContent wrapper and the inline QueuePage render — the page becomes Appointments-only content. Decide where the "Appointment & Queue" <h1> goes (drop it in favor of the header switcher, or reword to just "Appointments" on that page and "Queue" on the queue page) — I'd default to the latter unless you say otherwise.

Follow-up verification: From /appointments, header shows both links, "Appointments" marked active; click "Queue" → URL changes to /queue, content swaps, "Queue" now marked active; browser back/forward works; no local tab state left in appointments-page.tsx (grep for activeTab should return nothing there). Redo if the switcher doesn't reflect the actual route or old tab state remnants remain.

## Task Accomplished

- Updated `appointments-layout.tsx`: Added nav switcher with "Appointments" and "Queue" links using `useMatchRoute()` for active highlighting.
- Updated `appointments-page.tsx`: Removed `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` wrapper, removed `QueuePage` import/render, removed `activeTab` state, changed heading to "Appointments".
- Verified: `grep activeTab` returns nothing in appointments-page.tsx. No tab remnants.

## Files Changed

- `apps/clinic-ui/src/layouts/appointments-layout.tsx` — Added nav switcher with route-aware highlighting
- `apps/clinic-ui/src/modules/appointments/components/appointments-page.tsx` — Removed Tabs wrapper, QueuePage, activeTab state; updated heading

## Notes

None.
