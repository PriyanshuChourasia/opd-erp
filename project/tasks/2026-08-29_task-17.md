# Task 17 — Wire into Organisation sidebar

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 5 — Wire into the Organisation sidebar
Add three SidebarMenu rows in apps/api/prisma/seed.ts (alongside the existing Organisation-group entries around line 3372-3377): { label: 'Departments', path: '/organisation/departments', icon: 'Building2', group: 'Organisation', ... }, and same for Designations (icon e.g. UserCog) and Financial Years (icon e.g. CalendarClock), with sortOrder continuing after the existing 5. Re-run the seed (or the targeted upsert) so the rows exist in the dev DB.

Follow-up verification: Reload the app as an Admin user, confirm "Departments", "Designations", "Financial Years" appear under the Organisation sidebar group, in the intended order.

## Task Accomplished

- Added 3 SidebarMenu entries in seed.ts: Departments (sortOrder 6), Designations (sortOrder 7), Financial Years (sortOrder 8).
- Added fallback nav entries in app-sidebar.tsx for the Organisation group.
- Seeded sidebar menu rows into the dev DB via targeted upsert script.

## Files Changed

- `apps/api/prisma/seed.ts` — Added 3 sidebar menu entries
- `apps/clinic-ui/src/layouts/app-sidebar.tsx` — Added fallback nav entries for Departments, Designations, Financial Years

## Notes

None.
