# Task 18 — Schema rename fee → amount + migration

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 1 — Schema + migration
Rename fee to amount on the Appointment model in schema.prisma. Generate/apply a migration that does ALTER TABLE "Appointment" RENAME COLUMN "fee" TO "amount" (not drop+recreate, to preserve existing data).

Follow-up verification: npx prisma migrate status clean; existing appointment rows kept their fee values under the new column name.

## Task Accomplished

- Renamed `fee` to `amount` on the Appointment model in schema.prisma.
- Generated and applied migration `20260829_rename_appointment_fee_to_amount` with `ALTER TABLE "Appointment" RENAME COLUMN "fee" TO "amount"`.
- Prisma client regenerated. Migration status clean.

## Files Changed

- `apps/api/prisma/schema.prisma` — Renamed fee → amount on Appointment model
- `apps/api/prisma/migrations/20260829_rename_appointment_fee_to_amount/migration.sql` — ALTER TABLE RENAME COLUMN

## Notes

Data preserved — existing rows keep their values under the new column name.
