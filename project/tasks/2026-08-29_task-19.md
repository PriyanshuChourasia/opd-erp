# Task 19 — Backend rename fee → amount

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 2 — Backend rename
Update every .fee reference in apps/api/src/appointments/* and apps/api/src/queue/queue.service.ts to .amount, including the DTOs' fee?: number → amount?: number.

Follow-up verification: grep -rn "\.fee\b\|fee:" apps/api/src/appointments apps/api/src/queue returns nothing appointment-related.

## Task Accomplished

- Updated appointments.service.ts: all `.fee` → `.amount` (5 places).
- Updated create-appointment.dto.ts: `fee?: number` → `amount?: number`.
- Updated update-appointment.dto.ts: `fee?: number` → `amount?: number`.
- Updated queue.service.ts: all `fee: true` → `amount: true` in select clauses (4 places).
- Verified: grep returns no appointment-related `.fee` references.

## Files Changed

- `apps/api/src/appointments/appointments.service.ts` — Renamed fee → amount (5 places)
- `apps/api/src/appointments/dto/create-appointment.dto.ts` — fee → amount
- `apps/api/src/appointments/dto/update-appointment.dto.ts` — fee → amount
- `apps/api/src/queue/queue.service.ts` — Renamed fee → amount in select clauses (4 places)

## Notes

None.
