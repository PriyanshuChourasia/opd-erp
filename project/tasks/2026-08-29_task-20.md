# Task 20 — Frontend rename fee → amount

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 3 — Frontend rename
Update lib/api.ts's appointment type and every .fee/fee: usage in appointments-page.tsx, edit-appointment-page.tsx, new-appointment-page.tsx, receptionist-dashboard-page.tsx to amount.

Follow-up verification: grep -rn "\.fee\b\|fee:" apps/clinic-ui/src returns nothing appointment-related (allow registrationFee/consultationFee if separate).

## Task Accomplished

- Updated lib/api.ts: Appointment interface `fee` → `amount`, QueueEntry appointment select `fee` → `amount`.
- Updated appointments-page.tsx: all `.fee` → `.amount` (4 places).
- Updated edit-appointment-page.tsx: all `.fee` → `.amount` (7 places).
- Updated new-appointment-page.tsx: all `.fee` → `.amount`.
- Updated receptionist-dashboard-page.tsx: all `.fee` → `.amount` (12 places).
- Verified: grep returns no appointment-related `.fee` references. Build passes.

## Files Changed

- `apps/clinic-ui/src/lib/api.ts` — Appointment type fee → amount
- `apps/clinic-ui/src/modules/appointments/components/appointments-page.tsx` — fee → amount
- `apps/clinic-ui/src/modules/appointments/components/edit-appointment-page.tsx` — fee → amount
- `apps/clinic-ui/src/modules/appointments/components/new-appointment-page.tsx` — fee → amount
- `apps/clinic-ui/src/modules/receptionist/components/receptionist-dashboard-page.tsx` — fee → amount

## Notes

registrationFee and consultationFee left unchanged (separate fields).
