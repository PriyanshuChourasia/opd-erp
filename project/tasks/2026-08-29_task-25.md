# Task 25 — Every date picker defaults to current date

Date: 2026-08-29
Status: Completed

## Task Given

Every date picker will have default current date.

## Task Accomplished

- Updated `filterDate` state in 3 pages to default to `todayStr()` instead of `""`:
  - `appointments-page.tsx` — filterDate defaults to today
  - `queue-page.tsx` — filterDate defaults to today
  - `prescriptions-page.tsx` — filterDate defaults to today
- Reports pages already had sensible date defaults (first of month / today range).
- Reschedule date pickers left empty (they're triggered by user action, not filters).

## Files Changed

- `apps/clinic-ui/src/modules/appointments/components/appointments-page.tsx` — filterDate defaults to todayStr()
- `apps/clinic-ui/src/modules/queue/components/queue-page.tsx` — filterDate defaults to todayStr()
- `apps/clinic-ui/src/modules/prescriptions/components/prescriptions-page.tsx` — filterDate defaults to todayStr()

## Notes

RescheduleDate and cardStartDate/cardEndDate left as empty strings since they're not filter fields — they're triggered by user interaction.
