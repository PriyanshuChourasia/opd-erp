# Task 26 — Medicine catalog API client additions

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 1 — API client additions
In apps/clinic-ui/src/lib/api.ts: extend the Medicine interface with alias, groupId, unitId, openingStock, currentStock, group, unitMaster. Add MedicineGroup and Unit interfaces (id, name, description, isActive). Add fetchMedicineGroups() and fetchUnits() (unpaginated-style calls, large limit, for populating dropdowns) and createMedicine(input: CreateMedicineInput), mirroring the Shift API block's shape.

## Task Accomplished

- Extended Medicine interface with: alias, groupId, unitId, openingStock, currentStock, group (MedicineGroup), unitMaster (Unit).
- Added MedicineGroup interface (id, name, description, isActive).
- Added Unit interface (id, name, symbol, description, isActive).
- Added CreateMedicineInput type.
- Added createMedicine() — POST /medicine-catalog.
- Added fetchMedicineGroups() — GET /medicine-groups.
- Added fetchUnits() — GET /units.
- Added groupId param to fetchMedicines().
- All exports hit correct paths (/medicine-groups, /units, /medicine-catalog).

## Files Changed

- `apps/clinic-ui/src/lib/api.ts` — Added MedicineGroup, Unit interfaces, CreateMedicineInput, createMedicine, fetchMedicineGroups, fetchUnits functions

## Notes

None.
