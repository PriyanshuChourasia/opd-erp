# Task 27 — Add Medicine button + form

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 2 — Add Medicine button + form
In medicine-catalog-page.tsx, add a "+ Add Medicine" button in the header row (next to the title, matching the Shifts page's placement/style) that opens a Sheet with a form: name (required), price, category, alias, group (Select populated from fetchMedicineGroups), unit (Select from fetchUnits), openingStock. Wire to a useMutation(createMedicine) that invalidates the ["medicines"] query, closes the sheet, and toasts success/error via extractApiError — same pattern as shifts-page.tsx.

## Task Accomplished

- Added "+ Add Medicine" button in header row next to title.
- Added Sheet with form: name (required), alias, price, category, group (Select from fetchMedicineGroups), unit (Select from fetchUnits), openingStock.
- Wired to useMutation(createMedicine) with invalidation of ["medicines"] query, sheet close, and toast success/error.
- Added Group, Unit, Stock columns to the DataTable.
- Empty name validation blocks submit (button disabled when name is empty).

## Files Changed

- `apps/clinic-ui/src/modules/medicine-catalog/components/medicine-catalog-page.tsx` — Added Add Medicine button, Sheet form with group/unit selects, createMedicine mutation, new table columns

## Notes

None.
