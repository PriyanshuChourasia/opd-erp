# Task 28 — Keep only currently active option in solid color

Date: 2026-08-29
Status: Completed

## Task Given

Keep only the currently active option in solid color.

## Task Accomplished

- Scoped the CSS override to only target `[data-active][data-sidebar="menu-button"]` instead of the broad `[data-active]` selector.
- This ensures only the active sidebar menu button gets solid primary color, not hover states or other elements.

## Files Changed

- `apps/clinic-ui/src/index.css` — Scoped active color override to sidebar menu buttons only

## Notes

None.
