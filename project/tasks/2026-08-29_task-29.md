# Task 29 — Hide "Rescheduled" from the status filter

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 1 — Hide "Rescheduled" from the status filter
In appointments-page.tsx, remove the <option value="RESCHEDULED">Rescheduled</option> (line 758) from the "All statuses" <select>. Leave RESCHEDULED untouched everywhere else (APPT_STATUSES array, badge color map, the reschedule action itself) — this only hides it as a filter choice.

## Task Accomplished

Removed the `<option value="RESCHEDULED">Rescheduled</option>` from the status filter dropdown in appointments-page.tsx. All other RESCHEDULED references (APPT_STATUSES array, badge styles, reschedule flow) remain intact.

## Files Changed

- `apps/clinic-ui/src/modules/appointments/components/appointments-page.tsx` — Removed RESCHEDULED from filter dropdown options

## Notes

Verified that RESCHEDULED status still works in the table badges and status change dropdown.
