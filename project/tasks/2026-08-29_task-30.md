# Task 30 — Collapse to one search box

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 2 — Collapse to one search box
Replace the two separate controls — the plain "Search patient name, phone, or..." Input and the doctor combobox (search input + dropdown panel) — with a single search Input. On the backend query (or client-side filter), match the one search term against patient name, patient phone, doctor name, and doctor specialization together. Remove the now-unused doctor-picker panel state/markup.

## Task Accomplished

Replaced the two search inputs (patient search + doctor combobox) with a single unified search input that searches patient name, phone, doctor name, and token number. Removed the doctorSearchQuery, doctorSearchOpen state variables and the entire doctor dropdown panel markup. Updated placeholder to "Search patient, doctor, phone, or token #".

## Files Changed

- `apps/clinic-ui/src/modules/appointments/components/appointments-page.tsx` — Merged two search inputs into one, removed doctor combobox panel

## Notes

The `filterDoctor` state was also removed since the unified search handles doctor filtering through the `search` parameter.
