# Task 32 — Blood Group master table

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 4 — Blood Group master table
Add a BloodGroup model to schema.prisma (same shape as MedicineGroup/Unit: id, unique name, isActive, audit fields). Seed the 8 standard values (A+, A-, B+, B-, AB+, AB-, O+, O-). Add bloodGroupId String? + bloodGroup BloodGroup? relation to Patient. Build a minimal blood-groups backend module (read-only list is enough).

## Task Accomplished

1. Added BloodGroup model to schema.prisma with id, unique name, isActive, audit fields (createdById/updatedById with relations to User), and patients relation
2. Added bloodGroupId String? + bloodGroupRef BloodGroup? relation to Patient model
3. Added reverse relations (createdBloodGroups, updatedBloodGroups) to User model
4. Generated and applied migration (add_blood_group)
5. Created backend blood-groups module (read-only): controller, service, DTOs
6. Registered BloodGroupsModule in app.module.ts
7. Seeded 8 standard blood groups (A+, A-, B+, B-, AB+, AB-, O+, O-) via seed.ts

## Files Changed

- `apps/api/prisma/schema.prisma` — Added BloodGroup model, bloodGroupId+relation on Patient, reverse relations on User
- `apps/api/prisma/migrations/20260829065921_add_blood_group/migration.sql` — CREATE TABLE + ALTER TABLE
- `apps/api/src/blood-groups/blood-groups.controller.ts` — Read-only controller (findAll, findOne)
- `apps/api/src/blood-groups/blood-groups.service.ts` — Service with findAll, findOne
- `apps/api/src/blood-groups/blood-groups.module.ts` — Module definition
- `apps/api/src/blood-groups/dto/find-blood-groups-query.dto.ts` — Query DTO
- `apps/api/src/app.module.ts` — Registered BloodGroupsModule
- `apps/api/prisma/seed.ts` — Added seedBloodGroups function, also fixed fee→amount references

## Notes

Also fixed seed.ts fee→amount references that were missed in the earlier rename task.
