# Task 34 — Allergy chips inside the input (tag-input layout)

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 6 — Allergy chips inside the input
Rework AllergySelect so the selected-allergy badges render inside the same bordered field as the search input — a tag-input layout: chips inline, followed by the text cursor, all inside one visual box — instead of stacked above it.

## Task Accomplished

Reworked AllergySelect to use a tag-input layout. Selected allergy chips now render inside the same bordered container as the search input, with chips flowing inline followed by the text cursor. The container uses `flex-wrap` so it grows to fit chips, and clicking anywhere in the container focuses the input. Removed the separate `Input` component and replaced with a raw `<input>` for seamless inline flow.

## Files Changed

- `apps/clinic-ui/src/components/allergy-select/allergy-select.tsx` — Complete rewrite of layout to tag-input pattern with chips inside the bordered field

## Notes

Removed Input import since the search field is now a raw input element for inline chip flow.
