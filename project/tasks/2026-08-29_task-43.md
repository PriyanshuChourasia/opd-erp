# Task 43 — Sidebar header shows company name

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 1 — Sidebar header shows the company name:
In apps/clinic-ui/src/layouts/app-sidebar.tsx:181, the header hardcodes "MyClinic". Fetch the Organisation/Company record (the same one the /organisation settings page already reads via apps/api/src/organisation) and render its name field there instead, with "MyClinic" only as a fallback while loading or if unset.

Follow-up verification: Change the company name in Settings, reload the app — sidebar header updates to match. Clear/unset the name — sidebar falls back gracefully (no blank/undefined shown).

## Task Accomplished

- Added fetchOrganisation import and useQuery call in app-sidebar.tsx
- Sidebar header now renders organisation.name with "MyClinic" as fallback when loading or if name is empty/unset
- The existing /organisation endpoint is reused (no new backend endpoint needed)

## Files Changed

- apps/clinic-ui/src/layouts/app-sidebar.tsx — Added fetchOrganisation import, useQuery for organisation data, and dynamic name rendering in sidebar header

## Files Changed

- TODO

## Notes

- Need to add a GET /company endpoint (or reuse /organisation) to the backend
- Frontend sidebar needs to use react-query to fetch and display the company name
