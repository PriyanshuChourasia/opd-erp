# Task 47 — Fix Print Prescription preview

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 2 — Fix the Print Prescription preview:
Three concrete issues:
1. The Adv: row renders an empty dashed underline spanning the full width with nothing filled in — looks broken/unfinished when there's no advice text. Either populate it from an actual "Advice" field if one exists on the prescription, or hide the row entirely when there's no value, rather than always rendering an empty line.
2. The clinic phone/email (022-25551234 · info@cityclinic.com) is duplicated verbatim in both the header and the footer — redundant. Keep it in one place (footer is the more standard convention for prescription pads) and drop it from the header.
3. There's a large, oddly empty gap between the "Notes" section and the footer bar — tighten the vertical spacing so the content block doesn't float with dead space above the footer.

## Task Accomplished

- Fixed Adv: row to only show in design mode (no data prop), hidden when printing with real data
- Removed clinic phone/email from headers in all layout styles (classic, modern, banner, letterhead, prescription-pad)
- Kept clinic phone/email in footer only
- Updated PDF/Word export to show address only in header, phone/email in footer
- Reduced signature margin-top from 40px to 20px to tighten spacing

## Files Changed

- apps/clinic-ui/src/modules/prescription-templates/components/prescription-template-preview.tsx — Fixed Adv: row visibility, removed phone/email from all headers
- apps/clinic-ui/src/modules/prescriptions/components/prescriptions-page.tsx — Fixed PDF/Word export header and footer spacing

## Notes

- Adv: row now only shows in design mode (template editor), hidden when printing with real prescription data
- Clinic contact info appears exactly once in the footer across all layouts
- Signature spacing reduced to minimize dead space above footer
