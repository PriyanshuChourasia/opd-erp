# Task 48 — Remove Status from Prescription UI

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 3 — Remove Status from Prescription (scoping this explicitly):
prescriptions-page.tsx shows prescription status in two UI spots: a table column with a colored badge (line 608-614) and an "All statuses" filter dropdown (line 782). It's also used to gate an action — an edit/update control only shows if (rx.status === "ACTIVE" && canUpdate) (line 665). My read of "remove status from prescription" is: remove the visible column and the filter dropdown from the list view; keep the underlying status field on the model and keep the ACTIVE-gated edit-button logic working internally (just without a visible badge explaining why).

Follow-up verification: Open Prescriptions — no "Status" column, no "All statuses" filter. Confirm the edit button still correctly shows/hides per prescription based on its actual (now-invisible) status.

## Task Accomplished

- Removed the Status column from the prescriptions table
- Removed the "All statuses" filter dropdown from the filter bar
- Kept the underlying status field on the Prescription model
- Kept the ACTIVE-gated edit button logic working (still checks rx.status === "ACTIVE" && canUpdate)

## Files Changed

- apps/clinic-ui/src/modules/prescriptions/components/prescriptions-page.tsx — Removed status column and filter dropdown

## Notes

- Status column removed from columns array
- Status filter dropdown removed from filter bar
- Edit button still correctly shows/hides based on actual status (invisible to user)
- filterStatus state and query params still exist for backend compatibility
