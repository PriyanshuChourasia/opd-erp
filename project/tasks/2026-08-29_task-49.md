# Task 49 — Company profile tax/compliance fields

Date: 2026-08-29
Status: Completed

## Task Given

Prompt 4 — Company profile: tax/compliance fields + admin priority:
No gstNo/drugLicense/tax fields exist anywhere currently. Add to the Company model (post Organisation→Company rename from the prior prompt) statutory fields relevant to a pharmacy/clinic business: gstNumber String?, panNumber String?, drugLicenseNumber String?, drugLicenseExpiry DateTime?, taxRegistrationNumber String?. Add matching inputs to the Company Profile settings page. On "give profile ... more priority" — ensure the Company Profile settings page sits prominently for the Admin role (near the top of the Organisation sidebar group, and confirm Admin's permission set includes full read/write on it).

## Task Accomplished

- Added gstNumber, panNumber, drugLicenseNumber, drugLicenseExpiry, taxRegistrationNumber fields to Company model in schema.prisma
- Updated UpdateCompanyDto with new fields and validation decorators
- Updated Organisation interface and UpdateOrganisationInput in frontend api.ts
- Updated Company page with new fields in both display (Company Details card) and edit form (Tax & Compliance section)
- Company Profile already positioned prominently in Organisation sidebar group

## Files Changed

- apps/api/prisma/schema.prisma — Added 5 new nullable fields to Company model
- apps/api/src/company/dto/update-company.dto.ts — Added new fields with validation decorators
- apps/clinic-ui/src/lib/api.ts — Added new fields to Organisation and UpdateOrganisationInput interfaces
- apps/clinic-ui/src/modules/company/components/company-page.tsx — Added new fields to display and edit form

## Notes

- All new fields are nullable (String? or DateTime?) to avoid breaking existing data
- Drug license expiry stored as DateTime, displayed as date string
- Company Profile already has read:company and update:company permissions for Admin role
- Company Profile is first item in Organisation sidebar group
