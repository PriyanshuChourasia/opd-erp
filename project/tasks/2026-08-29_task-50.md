# Task 50 — Split schema.prisma into multiple files

Date: 2026-08-29
Status: In Progress

## Task Given

Prompt 5 — Split schema.prisma into multiple files:
apps/api/prisma/schema.prisma is a single ~1000+ line file covering every domain. Prisma version here is 5.22.0, which supports multi-file schemas as a preview feature (prismaSchemaFolder) — stable/no-flag-needed only from Prisma 6.7+, so this requires adding previewFeatures = ["prismaSchemaFolder"] to the generator client block first, then converting prisma/schema.prisma into a prisma/schema/ folder of multiple .prisma files. Split by bounded domain: auth.prisma, clinical.prisma, pharmacy.prisma, billing.prisma, company.prisma, system.prisma.

## Task Accomplished

- TODO

## Files Changed

- TODO

## Notes

- Need to add previewFeatures = ["prismaSchemaFolder"] to generator client
- Create prisma/schema/ folder
- Split into domain-specific files
- Validate with npx prisma validate and npx prisma generate
- Ensure no spurious migration is generated
