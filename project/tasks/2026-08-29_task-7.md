# Task 7 — Dashboard with shadcn sidebar in admin-ui

Date: 2026-08-29
Status: Completed

## Task Given

> now create a dashboard with sidebar of shadcn UI

## Task Accomplished

- Bootstrapped shadcn project config (`components.json`) in `apps/admin-ui` and added the **shadcn `sidebar`** component via the CLI (`npx shadcn add sidebar`), which also brought in `separator`, `sheet`, `tooltip`, `skeleton` + `use-mobile` hook and refreshed `button`/`input`/`index.css` (ships the `--sidebar-*` theme tokens, already mapped in `@theme`).
- Wrapped the app in `TooltipProvider` (required by `tooltip`/collapsed-sidebar tooltips).
- Restructured routes into a protected pathless layout:
  - `src/routes/_admin.tsx` — `SidebarProvider` + collapsible `Sidebar`: brand header (OPD ERP · Admin console), **Platform** group with active Dashboard link, **Administration** group (Users, Roles & permissions, Audit logs — "coming soon" toast), footer user card (skeleton while loading `GET /auth/me`) + sign-out button, `SidebarInset` with `SidebarTrigger` header and `<Outlet/>`. Guards unauthenticated visitors back to `/` via `beforeLoad`.
  - `src/routes/_admin/dashboard.tsx` — dashboard page: welcome, 4 stat cards (name/role/user ID/email from `GET /auth/me`), API-status card (auth endpoints + `https://admin-api.test/api`), demo-credentials card.
  - Removed the old standalone `src/routes/dashboard.tsx`.
- Refreshed `routeTree.gen.ts` (the `/dashboard` route now resolves to `/_admin/dashboard`). Verified `vite build` + `tsc --noEmit` + ESLint all clean, and a live dev-server boot serves the app + layout/sidebar modules without errors.

## Files Changed

- `apps/admin-ui/components.json` — new shadcn CLI config.
- `apps/admin-ui/src/components/ui/sidebar.tsx` — new (CLI, ~726 lines).
- `apps/admin-ui/src/components/ui/separator.tsx`, `sheet.tsx`, `tooltip.tsx`, `skeleton.tsx` — new (CLI siblings).
- `apps/admin-ui/src/hooks/use-mobile.ts` — new (CLI).
- `apps/admin-ui/src/components/ui/button.tsx`, `input.tsx`, `src/index.css` — auto-updated by the CLI (sidebar tokens, minor tweaks).
- `apps/admin-ui/src/routes/__root.tsx` — wrapped in `TooltipProvider`.
- `apps/admin-ui/src/routes/_admin.tsx` — new protected sidebar layout.
- `apps/admin-ui/src/routes/_admin/dashboard.tsx` — new dashboard page (moved from old route).
- `apps/admin-ui/src/routes/dashboard.tsx` — deleted (superseded by `_admin/dashboard`).
- `apps/admin-ui/src/routeTree.gen.ts` — regenerated (`/_admin` layout).
- `apps/admin-ui/dist/*` — rebuilt.
- `project/tasks/2026-08-29_task-7.md` — this record.

## Notes

- The sidebar only has one live navigation item (Dashboard) because the admin-api currently exposes only auth endpoints; the Administration entries are intentionally placeholders (toast "coming soon") until those APIs exist.
- `VITE_API_BASE=https://admin-api.test/api` (from task 6) is unchanged; the dashboard status card shows it.