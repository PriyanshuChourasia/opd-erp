Status: Completed

## Task Given

TASK 09 — Remove Allergies from New Appointment UI

Prompt: On /appointments/new, locate the Allergies section in the right-side grid of the New Appointment page. Remove the Allergies section from the UI.

Requirements:
- Remove only this section from the New Appointment page
- Do not delete the allergy feature from the entire application
- Do not delete allergy data from the database
- Do not modify patient records
- Reorganize the right-side grid so the layout remains clean
- Verify that Allergies no longer appears on /appointments/new

## Task Accomplished

- Removed the Allergies field from the left column (was in a 2-col grid with Doctor)
- Doctor field now takes full width (grid wrapper removed)
- Removed the Allergies display section from the right-side patient info card
- Removed unused `AllergySelect` import
- Removed unused `AlertTriangle` import
- Kept `allergies` field in `BookingForm` interface and mutation logic (syncs with patient record on save — preserves existing behavior without modifying patient records)

## Files Changed

- `apps/clinic-ui/src/modules/appointments/components/new-appointment-page.tsx`
  - Removed `AllergySelect` import
  - Removed `AlertTriangle` from lucide-react imports
  - Removed Allergies field from left column grid
  - Removed Allergies display from right-side patient info card
  - Doctor field now standalone (no longer in a 2-col grid with Allergies)

## Verification

| Check | Result |
|-------|--------|
| Allergies no longer on /appointments/new | ✅ Removed from both input and display |
| Allergy feature preserved elsewhere | ✅ Only removed from this page |
| No allergy data deleted | ✅ Database untouched |
| No patient records modified | ✅ Mutation logic preserved |
| Layout clean | ✅ Doctor field takes full width |
| Typecheck passes | ✅ |
