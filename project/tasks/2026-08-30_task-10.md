Status: Completed (Investigation Only — No Changes Made)

## Task Given

TASK 10 — Inspect existing patient vitals implementation

Prompt: Inspect the existing patient vitals functionality in the application. Determine whether a patient vitals database table already exists, existing model/entity, API endpoints, service/repository code, frontend components, and patient vitals fields. Do not create or modify anything yet. Return a concise summary.

## Task Accomplished

### Database Table: `PatientVitals` ✅ Exists

| Column | Type | Nullable |
|--------|------|----------|
| id | text (UUID) | PK |
| patientId | text | NOT NULL → Patient.id |
| appointmentId | text | nullable → Appointment.id (SET NULL) |
| heightCm | double precision | nullable |
| weightKg | double precision | nullable |
| bmi | double precision | nullable (auto-calculated) |
| temperatureC | double precision | nullable |
| pulseBpm | integer | nullable |
| systolicBp | integer | nullable |
| diastolicBp | integer | nullable |
| spo2Percent | double precision | nullable |
| respiratoryRate | integer | nullable |
| medicalStatus | text | nullable |
| recordedAt | timestamp | NOT NULL (default NOW) |
| createdById | text | nullable → User.id |

**92 records exist** (from earlier cleanup, all with null appointmentId).

---

### Backend: `apps/api/src/patient-vitals/`

**Controller** (`patient-vitals.controller.ts`):
| Method | Endpoint | Permission | Description |
|--------|----------|------------|-------------|
| GET | `/patient-vitals` | read:patient-vitals | List vitals (filter by patientId) |
| GET | `/patient-vitals/latest/:patientId` | read:patient-vitals | Get latest vitals for patient |
| GET | `/patient-vitals/range/:patientId` | read:patient-vitals | Get vitals in date range |
| GET | `/patient-vitals/:id` | read:patient-vitals | Get single vitals record |
| POST | `/patient-vitals` | create:patient-vitals | Record new vitals |

**No PATCH or DELETE** — vitals are immutable historical records.

**Service** (`patient-vitals.service.ts`):
- `create(dto, userId)` — creates vitals, auto-calculates BMI from height+weight
- `findByPatient(patientId, page, limit)` — paginated list
- `findLatest(patientId)` — most recent record
- `findOne(id)` — single record by ID
- `findByDateRange(patientId, from, to)` — date range query

**DTO** (`create-patient-vitals.dto.ts`):
- `patientId` (required)
- `appointmentId` (optional)
- `heightCm`, `weightKg`, `temperatureC`, `pulseBpm`, `systolicBp`, `diastolicBp`, `spo2Percent`, `respiratoryRate` (all optional numbers)
- `medicalStatus` (optional string)
- `recordedAt` (optional date)

---

### Frontend: `apps/clinic-ui/src/lib/api.ts`

**Types**:
- `PatientVitals` interface (lines 997-1013)
- `CreatePatientVitalsInput` interface (lines 1015-1026)
- `MEDICAL_STATUSES` constant (lines 1028-1045)

**API Functions**:
- `createPatientVitals(input)` — POST `/patient-vitals`
- `fetchPatientVitalsLatest(patientId)` — GET `/patient-vitals/latest/:patientId`
- `fetchPatientVitalsHistory(patientId, limit)` — GET `/patient-vitals`

---

### Existing Frontend Usage

1. **Appointments page** (`appointments-page.tsx`):
   - HeartPulse button opens a Vitals Sheet (slide-in panel)
   - Fetches `latestVitals` for selected patient
   - Shows latest vitals read-only, or a form to record new ones
   - `vitalsMutation` calls `createPatientVitals()` with `appointmentId`

2. **New Appointment page** (`new-appointment-page.tsx`):
   - Fetches `latestVitals` for selected patient (line 151-153)
   - Displays latest vitals in the right-side Patient Info card (lines 752-770)
   - Read-only display: Height, Weight, BMI, Temp, Pulse, BP, SpO₂, Resp Rate

3. **Patient form sheet** (`patient-form-sheet.tsx`):
   - Fetches `latestVitals` for editing patient
   - Can record vitals when saving patient changes

4. **Patients page** (`patients-page.tsx`):
   - Can create vitals for patients

---

### Reusable for New Appointment Page

| Component | Reusable? | Notes |
|-----------|-----------|-------|
| `PatientVitals` type | ✅ | Already imported and used |
| `CreatePatientVitalsInput` type | ✅ | Already imported and used |
| `createPatientVitals()` API | ✅ | Already used in appointments-page.tsx |
| `fetchPatientVitalsLatest()` API | ✅ | Already used in new-appointment-page.tsx |
| `MEDICAL_STATUSES` constant | ✅ | Available for status dropdown |
| Vitals form fields (8 fields) | ✅ | Same fields used everywhere |
| BMI auto-calculation | ✅ | Backend handles this automatically |
| Vitals display grid | ✅ | Already rendered in new-appointment-page.tsx (lines 752-770) |

**Everything needed already exists.** The vitals infrastructure is complete — database, API, types, and display. Any new vitals-related feature on the New Appointment page can reuse these directly.
