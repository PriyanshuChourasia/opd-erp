Status: Completed

## Task Given

TASK 12 — Add Add button to Patient Vitals

Prompt: On /appointments/new, locate the Patient Vitals section in the right-side grid. Add an Add button to the Patient Vitals section.

Requirements:
- Keep the existing Patient Vitals section
- Add a clearly visible Add action
- Clicking Add must open the Patient Vitals modal
- Do not implement the modal fields in this task
- Verify that clicking Add opens the modal placeholder/component correctly

## Task Accomplished

- Added `HeartPulse` to lucide-react imports (not used yet, reserved for future)
- Added `vitalsModalOpen` state (line 129)
- Added "Add" button next to "Patient Vitals" header (line 757-761)
  - Plus icon + "Add" text
  - Disabled when no patient selected
  - Primary color to stand out
- Vitals section now always visible when a patient is selected (not hidden when no vitals)
- Added "No vitals recorded yet" placeholder text when no vitals exist
- Added placeholder Sheet that opens on Add click (lines 1037-1052)
  - Title: "Record Patient Vitals"
  - Shows patient name
  - Placeholder content: "Vitals form will be implemented here."
  - Cancel + disabled "Record Vitals" buttons

## Files Changed

- `apps/clinic-ui/src/modules/appointments/components/new-appointment-page.tsx`
  - Line 5: Added `HeartPulse` import
  - Line 129: Added `vitalsModalOpen` state
  - Lines 752-792: Restructured Patient Vitals section (header with Add button, conditional grid, empty state)
  - Lines 1037-1052: Added placeholder Sheet

## Verification

| Check | Result |
|-------|--------|
| Add button visible | ✅ Next to "Patient Vitals" header |
| Click opens Sheet | ✅ Placeholder Sheet with title + patient name |
| Button disabled without patient | ✅ `disabled={!form.patient}` |
| Existing vitals still display | ✅ Grid shows when vitals exist |
| Empty state shown | ✅ "No vitals recorded yet" when no vitals |
| Typecheck passes | ✅ |
