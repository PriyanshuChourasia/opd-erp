Status: Completed

## Task Given

TASK 13 — Create Patient Vitals modal

Prompt: Create the Patient Vitals modal for /appointments/new. Use the vitals fields identified in the previous task.

Requirements:
- Modal should have a clear title
- Display all required vitals fields
- Provide Save and Cancel actions
- Validate required fields
- Keep the modal consistent with the application's existing UI components
- Do not change unrelated New Appointment fields
- Do not implement backend persistence yet

## Task Accomplished

- Added vitals state (`vitals`) with all 9 fields (same pattern as appointments-page.tsx)
- Added `computedBmi` useMemo for auto-calculating BMI from height+weight
- Replaced placeholder Sheet with full vitals form

### Form Structure

**Title:** "Record Patient Vitals" + patient name

**Section 1: Body Measurements**
- Height (cm) — number input, placeholder "170"
- Weight (kg) — number input, placeholder "70"
- BMI — auto-calculated display (shown only when both height and weight are provided)

**Section 2: Vital Signs**
- Temperature (°F) — number input, step 0.1, placeholder "98.6"
- Pulse (bpm) — number input, placeholder "72"
- Systolic BP (mmHg) — number input, placeholder "120"
- Diastolic BP (mmHg) — number input, placeholder "80"
- SpO₂ (%) — number input, placeholder "98"
- Resp. Rate (/min) — number input, placeholder "16"

**Section 3: Clinical Context**
- Medical Status — dropdown with 15 options from MEDICAL_STATUSES

**Footer:** Cancel (resets form) + Record Vitals (disabled — backend not yet wired)

### Design Decisions
- All fields optional (vitals are partial — matches backend DTO)
- BMI auto-calculated client-side (matches backend behavior)
- Same field layout as existing vitals form in appointments-page.tsx
- Form resets on close/cancel

## Files Changed

- `apps/clinic-ui/src/modules/appointments/components/new-appointment-page.tsx`
  - Lines 130-141: Added vitals state + computedBmi
  - Lines 1050-1150: Replaced placeholder Sheet with full vitals form

## Verification

| Check | Result |
|-------|--------|
| Title clear | ✅ "Record Patient Vitals" + patient name |
| All fields present | ✅ 9 fields + BMI display |
| BMI auto-calculates | ✅ useMemo recalculates on height/weight change |
| Cancel resets form | ✅ All fields cleared on close |
| Consistent with existing UI | ✅ Same pattern as appointments-page.tsx vitals form |
| No unrelated changes | ✅ |
| Typecheck passes | ✅ |
