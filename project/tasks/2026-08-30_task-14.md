Status: Completed

## Task Given

TASK 14 — Connect Patient Vitals modal to backend

Prompt: Connect the Patient Vitals modal on /appointments/new to the existing patient vitals backend functionality.

Requirements:
- Submit the entered vitals using the existing API/service where possible
- Associate the vitals with the selected patient
- Handle loading state
- Handle API errors
- Show successful save state
- Refresh/display the newly added vitals in the Patient Vitals section
- Do not modify unrelated appointment functionality

## Task Accomplished

- Added `createPatientVitals` to api imports (line 19)
- Added `hasVitalsData` useMemo — checks if any vitals field has a value (lines 144-147)
- Added `vitalsMutation` — calls `createPatientVitals` with form data (lines 149-172)
  - Builds payload from vitals state (same pattern as appointments-page.tsx)
  - Associates with `form.patient.id`
  - Auto-calculated BMI is sent by backend
- Updated "Record Vitals" button:
  - Disabled when no vitals data or mutation is pending
  - Shows "Saving..." during submission
  - Calls `vitalsMutation.mutate()` on click
- On success: invalidates `patientVitals` query (refreshes display), shows toast, closes sheet, resets form
- On error: shows error toast via `extractApiError`

## Files Changed

- `apps/clinic-ui/src/modules/appointments/components/new-appointment-page.tsx`
  - Line 19: Added `createPatientVitals` import
  - Lines 144-172: Added `hasVitalsData` + `vitalsMutation`
  - Lines 1184-1186: Updated Record Vitals button

## Verification

| Check | Result |
|-------|--------|
| Submit calls API | ✅ `createPatientVitals()` with patientId + vitals fields |
| Loading state | ✅ Button shows "Saving..." when `vitalsMutation.isPending` |
| Error handling | ✅ `toast.error(extractApiError(err))` |
| Success handling | ✅ Toast + close sheet + reset form |
| Refresh vitals display | ✅ `invalidateQueries(["patientVitals"])` re-fetches latest |
| Button disabled when empty | ✅ `!hasVitalsData` check |
| No unrelated changes | ✅ |
| Typecheck passes | ✅ |
