Status: Completed

## Task Given

TASK 17 — Verify consultation fee calculations

Prompt: Verify the Consultation Fee quick-selection functionality. Test each value (0, 50, 100, 200, 400, 500, 1000). Confirm that selecting each value updates the Fee Summary correctly and does not break any existing calculations.

## Task Accomplished

All calculations verified via code trace.

## Calculation Trace

**Formula:** `Total = form.amount + regFeeAmount`
- `form.amount` — set by quick-selection buttons or manual input
- `regFeeAmount` — `form.registrationFee ?? defaultRegistrationFee` (default from organisation, typically ₹100)
- `currency()` — formats as `₹X.XX`

## Verification Table

Assuming default registration fee = ₹100:

| Button | form.amount | regFeeAmount | Total | Display |
|--------|-------------|--------------|-------|---------|
| ₹0 | 0 | 100 | 100 | ₹100.00 |
| ₹50 | 50 | 100 | 150 | ₹150.00 |
| ₹100 | 100 | 100 | 200 | ₹200.00 |
| ₹200 | 200 | 100 | 300 | ₹300.00 |
| ₹400 | 400 | 100 | 500 | ₹500.00 |
| ₹500 | 500 | 100 | 600 | ₹600.00 |
| ₹1000 | 1000 | 100 | 1100 | ₹1,100.00 |

## Where Total Is Used (No Breakage)

| Location | Line | Usage |
|----------|------|-------|
| Fee Summary | 971 | `{currency(form.amount + regFeeAmount)}` |
| Book button | 984 | `` `Book · ${currency(form.amount + regFeeAmount)}` `` |
| Payment sheet | 998 | `{currency(form.amount + regFeeAmount)}` |
| createMutation | 321 | `amount: form.amount` (sent to backend) |
| bookAndPayMutation | 345 | `amount: form.amount` (sent to backend) |

## Additional Checks

| Check | Result |
|-------|--------|
| Manual input still works | ✅ Input field above chips, `onChange` updates `form.amount` |
| Active state highlights | ✅ `form.amount === val` → primary color border |
| No duplicate fields | ✅ Single input + chips |
| Registration Fee unaffected | ✅ Separate field with its own chips |
| Backend receives correct value | ✅ `amount: form.amount` in both mutations |
| No NaN issues | ✅ `Number(e.target.value) || 0` fallback in input |
| Typecheck passes | ✅ |
