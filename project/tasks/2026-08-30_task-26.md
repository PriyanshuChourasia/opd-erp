# Task 26 — Add Card transaction/invoice number

**Status:** Complete
**Date:** 2026-08-30

## Task Given

In the Book & Pay payment modal, when user selects Card, display an input for Invoice Number or Transaction Number.
- Field only appears when Card is selected
- Use existing payment data model if available
- Allow manual entry
- Do not display for UPI

## Task Accomplished

### Changes Made

| What | Where |
|------|-------|
| Added `referenceNumber String?` to Bill model | `schema.prisma` |
| Added `referenceNumber?: string` to CheckoutAppointmentDto | `checkout-appointment.dto.ts` |
| Added `referenceNumber?: string` to PaymentPayload | `payment-sheet.tsx` |
| Added `referenceNumber` state + input | `payment-sheet.tsx` — only shows when `method === "CARD"` |
| Updated `bookAndPayMutation` to pass referenceNumber | `new-appointment-page.tsx`, `edit-appointment-page.tsx`, `receptionist-dashboard-page.tsx` |
| Updated `checkoutAppointment` API signature | `api.ts` |
| Updated checkout service to save referenceNumber | `appointments.service.ts` |
| Created migration | `20260830_add_bill_reference_number` |

### Payment Modal Behavior

| Payment Method | Shows Reference Number Field? |
|----------------|------------------------------|
| Cash | ❌ No |
| Card | ✅ Yes — "Invoice / Transaction Number" |
| UPI | ❌ No |

### Data Flow

```
User selects Card → enters reference number
  → "Confirm & Book" clicked
    → bookAndPayMutation.mutate(payload)
      → POST /appointments/:id/checkout
        → { paymentMethod: "CARD", referenceNumber: "TXN-12345", ... }
      → Bill created with referenceNumber saved
```

### Preserved

- Cash and UPI flows unchanged
- All existing checkout functionality untouched
- `referenceNumber` is nullable — existing bills unaffected
