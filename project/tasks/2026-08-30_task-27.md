# Task 27 — Add UPI transaction/invoice number

**Status:** Complete
**Date:** 2026-08-30

## Task Given

In the Book & Pay payment modal, when user selects UPI, display an input for Invoice Number or Transaction Number.
- Field only appears when UPI is selected
- Reuse same reference field/model as Card
- Allow manual entry
- Do not display Card-specific UI when UPI is selected

## Task Accomplished

### Change Made

| What | Where |
|------|-------|
| Updated condition from `method === "CARD"` to `(method === "CARD" \|\| method === "UPI")` | `payment-sheet.tsx:99` |

One line change — the same `referenceNumber` field, state, and payload plumbing from TASK 26 now shows for both Card and UPI.

### Payment Modal Behavior

| Payment Method | Shows Reference Number Field? |
|----------------|------------------------------|
| Cash | ❌ No |
| Card | ✅ Yes |
| UPI | ✅ Yes (new) |
