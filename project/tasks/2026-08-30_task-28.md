# Task 28 — Persist payment reference number

**Status:** Already Implemented (TASK 26)
**Date:** 2026-08-30

## Task Given

Persist the Card/UPI Invoice Number or Transaction Number entered in the payment modal with the payment record.

## Task Accomplished

This was fully implemented in TASK 26. Verified the complete chain:

### Database

- `Bill.referenceNumber String?` — nullable column in `Bill` table
- Migration `20260830_add_bill_reference_number` applied

### Backend

- `CheckoutAppointmentDto.referenceNumber?: string` — validated input
- `appointments.service.ts checkout()` — saves `referenceNumber: dto.referenceNumber ?? null` on Bill creation

### Frontend

- `PaymentPayload.referenceNumber?: string` — in interface
- `PaymentSheet` — state + input (shown for Card and UPI)
- All 3 callers pass `referenceNumber` through: `new-appointment-page.tsx`, `edit-appointment-page.tsx`, `receptionist-dashboard-page.tsx`
- `checkoutAppointment()` API function accepts `referenceNumber`

### Backward Compatibility

- Column is nullable — existing bills unaffected
- `referenceNumber ?? null` in service — defaults to null if not provided
- Conditional spread `...(payload.referenceNumber ? { referenceNumber: payload.referenceNumber } : {})` — not sent if empty
