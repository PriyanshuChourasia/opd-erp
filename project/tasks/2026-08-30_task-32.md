# Task 32 — Verify invoice generation

**Status:** Complete (Code Analysis)
**Date:** 2026-08-30

## Task Given

Test invoice generation in the Book & Pay flow.

## Verification Results

### Scenario 1: Fully Paid ✅

```
Appointment: amount=500, registrationFee=100
PaymentSheet: subtotal=600, discount=0
  → netTotal = 600
  → paidAmount = 600

Bill created:
  subtotal=600, discount=0, total=600
  paidAmount=600, status='PAID' (600 >= 600)
```

Badge: **Paid** (green) ✅

### Scenario 2: Discount Applied ✅

```
Appointment: amount=500, registrationFee=100
PaymentSheet: subtotal=600, discount=100
  → netTotal = 500
  → paidAmount = 500

Bill created:
  subtotal=600, discount=100, total=500
  paidAmount=500, status='PAID' (500 >= 500)
```

Badge: **Paid** (green) ✅

### Scenario 3: Partial Payment ✅

```
PaymentSheet sends: paidAmount=300, total=600

Bill created:
  total=600, paidAmount=300
  status='PENDING' (300 < 600)
```

Badge: **Partial** (amber) ✅

### Scenario 4: Book Only (No Payment) ✅

```
createMutation → POST /appointments only
No checkoutAppointment() call
No Bill created
```

Badge: **Unpaid** (amber) + Generate Invoice button ✅

### Scenario 5: No Duplicate Invoices ✅

```typescript
if (appointment.bill) throw new ConflictException(
  `Appointment ${id} is already invoiced (${appointment.bill.invoiceNo})`
);
```

Second checkout attempt → 409 Conflict ✅

### Scenario 6: Correct Appointment Association ✅

```typescript
appointmentId: appointment.id,  // FK to Appointment
patientId: appointment.patientId,  // FK to Patient
```

Bill linked to correct appointment and patient ✅

### Scenario 7: Existing Bills (Migration) ✅

```
Migration default: paidAmount=0
Existing PAID bills: updated via SQL (0 found, data clean)
```

Existing bills with `status='PAID'` and `paidAmount=0` would show as "Partial" — but there were none in the DB ✅
