# Task 45 — Rename Fee to Amount in Excel export

**Status:** Already Implemented (No changes needed)
**Date:** 2026-08-30

## Task Given

Update the appointment Excel export so that the column currently named Fee is renamed to Amount.

## Verification

The Excel export already uses "Amount" as the column header (line 224):

```typescript
function exportToExcel() {
  const rows = (appointmentsResponse?.data ?? []).map((appt) => ({
    "Patient Name": `${appt.patient.firstName} ${appt.patient.lastName}`,
    "Phone": appt.patient.contactNo ?? "",
    "Doctor": appt.doctor.name ?? appt.doctor.medicalRegistrationNo ?? "",
    "Specialization": appt.doctor.specialization ?? "",
    "Date": appt.date ? new Date(appt.date).toLocaleDateString() : "",
    "Type": appt.type ?? "",
    "Status": appt.status,
    "Amount": appt.amount ?? 0,           // ← Already "Amount"
    "Registration Fee": appt.registrationFee ?? 0,
    "Token": appt.tokenNumber ?? "",
  }));
}
```

## Excel Columns (Verified)

| Column | Value |
|--------|-------|
| Patient Name | `firstName + lastName` |
| Phone | `contactNo` |
| Doctor | `name` or `medicalRegistrationNo` |
| Specialization | `specialization` |
| Date | `date` formatted |
| Type | `type` |
| Status | `status` |
| **Amount** | `amount` ✅ |
| Registration Fee | `registrationFee` |
| Token | `tokenNumber` |

No Action column included. ✅
