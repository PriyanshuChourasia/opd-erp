# Task 60 — Verify current vitals after deletion

**Status:** Complete (Code Analysis)
**Date:** 2026-08-30

## Lifecycle Trace

### Step 1: Create Vitals A

```
POST /patient-vitals { patientId: "xyz", heightCm: 170, weightKg: 65 }
→ DB: { id: "A", patientId: "xyz", heightCm: 170, deletedAt: null, deletedById: null }
```

### Step 2: Verify Vitals A is current

```
GET /patient-vitals/latest/xyz
→ findLatest: WHERE patientId = 'xyz' AND deletedAt = null ORDER BY recordedAt DESC
→ Returns: Vitals A ✅
```

### Step 3: Create Vitals B

```
POST /patient-vitals { patientId: "xyz", heightCm: 172, weightKg: 68 }
→ DB: { id: "B", patientId: "xyz", heightCm: 172, deletedAt: null, deletedById: null }
```

### Step 4: Verify Vitals B is current

```
GET /patient-vitals/latest/xyz
→ findLatest: WHERE patientId = 'xyz' AND deletedAt = null ORDER BY recordedAt DESC
→ Returns: Vitals B (most recent) ✅
```

### Step 5: Delete Vitals B

```
DELETE /patient-vitals/B
→ remove(id: "B", deletedById: "user-123")
→ UPDATE PatientVitals SET deletedAt = NOW(), deletedById = 'user-123' WHERE id = 'B'
→ DB: { id: "B", ..., deletedAt: 2026-08-30T10:00:00Z, deletedById: "user-123" }
```

### Step 6: Verify Vitals B excluded from active

```
GET /patient-vitals/latest/xyz
→ findLatest: WHERE patientId = 'xyz' AND deletedAt = null ORDER BY recordedAt DESC
→ Returns: Vitals A (B filtered out by deletedAt: null) ✅
```

### Step 7: Verify Vitals A is current

```
GET /patient-vitals/latest/xyz
→ Returns: Vitals A ✅
```

### Step 8: Verify Vitals B exists in DB with deleted_at/deleted_by

```sql
SELECT id, deletedAt, deletedById FROM "PatientVitals" WHERE id = 'B';
→ { id: 'B', deletedAt: '2026-08-30 10:00:00', deletedById: 'user-123' }
→ Record preserved, NOT physically deleted ✅
```

## Code Verification

| Check | Status | Evidence |
|-------|--------|----------|
| findLatest filters deleted | ✅ | `where: { patientId, deletedAt: null }` (line 99) |
| findByPatient filters deleted | ✅ | `where: { patientId, deletedAt: null }` (line 67) |
| findByDateRange filters deleted | ✅ | `where: { patientId, deletedAt: null, ... }` (line 113) |
| remove sets deletedAt | ✅ | `deletedAt: new Date()` (line 129) |
| remove sets deletedById | ✅ | `deletedById: deletedById ?? null` (line 129) |
| No physical DELETE | ✅ | Uses UPDATE, not DELETE |
