# Task 63 — Inspect Register Patient address implementation

**Status:** Complete
**Date:** 2026-08-30

## Patient Model Address Fields

The Patient model has a single `address String?` field (line 509).

## Address Model (Separate Table)

A full `Address` model exists (line 944) with:

| Field | Type | Required |
|-------|------|----------|
| addressType | String | ✅ |
| addressLine1 | String | ✅ |
| addressLine2 | String? | ❌ |
| landmark | String? | ❌ |
| city | String? | ❌ |
| district | String? | ❌ |
| state | String? | ❌ |
| country | String | Default: "India" |
| postalCode | String? | ❌ |
| latitude | Float? | ❌ |
| longitude | Float? | ❌ |
| isPrimary | Boolean | Default: false |
| addressableType | String | ✅ (polymorphic) |
| addressableId | String | ✅ (polymorphic) |

## Register Patient Form Address Fields

The form already has an "Add Address" section (line 364) with:

- Address Line 1 * (required)
- Address Line 2
- City
- District
- State (dropdown with INDIAN_STATES)
- Country
- Postal Code

## Existing Address API

| Function | Endpoint |
|----------|----------|
| `createAddress(input)` | POST `/addresses` |
| `AddressManager` component | Shows/manages addresses for any entity |

## Findings

**The Add Address option already exists in the Register Patient form.** It includes:
- Address Line 1, Address Line 2, City, District, State, Country, Postal Code
- State uses a dropdown with Indian states
- Address is saved via `createAddress()` API
- `AddressManager` component shows existing addresses for editing

### What's Missing

| Feature | Status |
|---------|--------|
| Landmark field | ❌ Not in form (exists in Address model) |
| Address Type selector | ❌ Not in form (defaults to "HOME") |
| Primary address toggle | ❌ Not in form |

### No Changes Needed

The core address functionality is already implemented. The form shows/hides an address section with all required fields.
