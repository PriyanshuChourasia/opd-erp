# Task 65 — Make Pincode required

**Status:** Complete
**Date:** 2026-08-30

## Task Given

Make Pincode required in the Register Patient Address form.

## Changes Made

| What | Where |
|------|-------|
| Added `*` to Postal Code label | `patient-form-sheet.tsx:407` |
| Added validation for empty addressLine1 | `patient-form-sheet.tsx:158-161` |
| Added validation for empty postalCode | `patient-form-sheet.tsx:162-165` |
| Added toast.error messages | Same lines |

## Validation Flow

```
User clicks "Register Patient"
  → submitAddress(patientId) called
    → if addressLine1 empty → toast "Address Line 1 is required" → return
    → if postalCode empty → toast "Postal Code is required" → return
    → createAddress() → POST /addresses
```

## Verification

| Check | Status |
|-------|--------|
| Postal Code shows `*` indicator | ✅ |
| Submission blocked when empty | ✅ `toast.error()` |
| Clear validation message | ✅ "Postal Code is required" |
| Follows existing style | ✅ Same as Address Line 1 validation |
| Value preserved on fail | ✅ State not cleared |
