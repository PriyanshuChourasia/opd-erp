# Task 67 — Verify Register Patient address

**Status:** Complete (Code Analysis)
**Date:** 2026-08-30

## Verification Results

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | Add Address available | ✅ | Line 372: Toggle button "Add Address" / "Hide" |
| 2 | Address can be entered | ✅ | 8 fields: Line 1, Line 2, Landmark, City, District, State, Country, Postal Code |
| 3 | Pincode required | ✅ | Line 407: "Postal Code *" label |
| 4 | Empty Pincode prevents submission | ✅ | Line 161-164: `toast.error("Postal Code is required")` |
| 5 | State defaults to West Bengal | ✅ | Line 50: `state: "West Bengal"` |
| 6 | State can be changed | ✅ | Line 395-405: State dropdown with INDIAN_STATES |
| 7 | Address saved correctly | ✅ | Line 166-171: `createAddress()` → POST `/addresses` |
| 8 | Existing registration intact | ✅ | Address submission is separate from patient creation |

## Complete Flow

```
User clicks "Add Address"
  → Address form expands
  → State pre-filled: "West Bengal"

User fills address
  → Address Line 1 * (required)
  → Address Line 2
  → Landmark
  → City
  → District
  → State (dropdown, default: West Bengal)
  → Country (default: India)
  → Postal Code * (required)

User clicks "Register Patient"
  → createPatient() → POST /patients
  → submitAddress(patientId)
    → validate: addressLine1 required
    → validate: postalCode required
    → createAddress() → POST /addresses
```
